#!/usr/bin/env ruby

puts "Thanks for choosing fine_woodworking magazine!"

puts "Here are the feautered articles today:"
puts "1. From the bench: The family violin"
puts "2. Tool review: Mega flush trim router bits by infinity"
puts "3. Sketchup: Creating cutaway views"
puts "4. Tapered sliding dovetails by hand"
puts "Select which article you would like to read?"

user_input = gets.strip

while user_input != "exit"

  case user_input
  when user_input = "1"
    puts "You selected article 1"
  when user_input = "2"
    puts "You selected article 2"
  when user_input = "3"
    puts "You selected article 3"
  end
end
